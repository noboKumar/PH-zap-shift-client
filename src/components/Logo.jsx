import React from 'react';
import logo from '../assets/logo.png';

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="logo" />
            <h1>Zap Shift</h1>
        </div>
    );
};

export default Logo;